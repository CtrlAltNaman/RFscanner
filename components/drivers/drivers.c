#include "device_board.h"

/*
 * This component is intentionally small. It acts as the central place for
 * hardware pin assignments so the rest of the firmware does not hardcode
 * board details across many files.
 */
